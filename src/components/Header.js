import React from 'react';
import '../styles/Header.css'; // Create a separate CSS file for styling

function Header() {
  return (
    <header className="app-header">
      <h1>PaddyGrow</h1>
    </header>
  );
}

export default Header;
